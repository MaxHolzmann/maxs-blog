<div>
    <h1>{{blog.title}}</h1>
    <h2>{{blog.body}}</h2>
    {{#each blog.comments as |comment|}}
    <h3>{{comment.comment_body}}</h3>
    <h3>{{comment.comment_username}}</h3>
    {{/each}}
</div>